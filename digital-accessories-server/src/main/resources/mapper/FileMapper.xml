<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.FileMapper">

    <insert id="insert" parameterType="com.example.entity.FileInfo">
        insert into `file_info` (name, url) values (#{name}, #{url})
    </insert>

    <select id="selectAll" resultType="com.example.entity.FileInfo">
        select * from `file_info`
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
        </where>
        order by id desc
    </select>

    <delete id="deleteById">
        delete from `file_info` where id = #{id}
    </delete>

</mapper>